# ── Brand / Contacto (público) ────────────────────────────────────────────
PUBLIC_BRAND_NAME="Landing Boilerplate"
PUBLIC_CONTACT_EMAIL="hola@tu-dominio.com"
PUBLIC_CONTACT_PHONE="+51 999 999 999"

# WhatsApp (opcional; sin + en el número, con código de país)
PUBLIC_WHATSAPP_NUMBER="51999999999"
PUBLIC_WHATSAPP_MSG="Hola, quiero más información"

# Sitio / SEO
PUBLIC_SITE_URL="https://tu-dominio.com"   # usado por sitemap/canonicals
PUBLIC_GA_ID=""                             # ej: G-XXXXXXX (opcional)

# i18n (opcional)
PUBLIC_DEFAULT_LANG="es"
PUBLIC_SUPPORTED_LANGS="es,en"              # coma separada

# ── Theming ───────────────────────────────────────────────────────────────
# Paleta/tenant: default | single-hex | multicolor-hex
PUBLIC_TENANT="default"

# Modo de color (superficies): light | dark | auto
PUBLIC_COLOR_MODE="light"

# Compatibilidad con tu variable anterior (opcional):
# - Si vale default/single-hex/multicolor-hex → lo tomamos como TENANT
# - Si vale light/dark/auto → lo tomamos como COLOR MODE
PUBLIC_THEME=""   # déjalo vacío si ya usas las dos de arriba

PUBLIC_INDEXING=false
